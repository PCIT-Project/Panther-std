////////////////////////////////////////////////////////////////////////////////////
//                                                                                //
// Part of Panther-STD, under the BSD 1-Clause License with PCIT Exceptions.      //
// You may not use this file except in compliance with the License.               //
// See `https://github.com/PCIT-Project/Panther-std/blob/main/LICENSE`for info.   //
//                                                                                //
////////////////////////////////////////////////////////////////////////////////////


@setModuleName("std.platform");

def std = @import("std");


when switch(@config.platform){
	case(std.Platform.FREESTANDING){
		def linux = delete("Current target platform is freestanding");
		def panther = delete("Current target platform is freestanding");
		def windows = delete("Current target platform is freestanding");

		def SUPPORTS_CMD: Bool = false;
		def SUPPORTS_NON_CMD_IO: Bool = false;
		def SUPPORTS_NON_LINEAR_HEAP: Bool = false;
	}

	case(std.Platform.LINUX){
		def linux = @import("./Linux/Linux.pthr");
		def panther = delete("Current target platform is Linux");
		def windows = delete("Current target platform is Linux");

		def SUPPORTS_CMD: Bool = true;
		def SUPPORTS_NON_CMD_IO: Bool = true;
		def SUPPORTS_NON_LINEAR_HEAP: Bool = true;
	}

	case(std.Platform.PANTHER_VM){
		def linux = delete("Current target platform is PantherVM");
		def pantherVM = @import("./PantherVM/PantherVM.pthr");
		def windows = delete("Current target platform is PantherVM");

		def SUPPORTS_CMD: Bool = true;
		def SUPPORTS_NON_CMD_IO: Bool = true;
		def SUPPORTS_NON_LINEAR_HEAP: Bool = true;
	}

	case(std.Platform.WINDOWS){
		def linux = delete("Current target platform is Windows");
		def panther = delete("Current target platform is Windows");
		def windows = @import("./Windows/Windows.pthr");

		def SUPPORTS_CMD: Bool = true;
		def SUPPORTS_NON_CMD_IO: Bool = true;
		def SUPPORTS_NON_LINEAR_HEAP: Bool = true;
	}
}

